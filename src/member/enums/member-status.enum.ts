export enum MemberStatus {
  PENDING = 'pending', // members who apply
  INVITED = 'invited', // members who are invited
  ACCEPTED = 'accepted',
  DECLINED = 'declined',
  EXPIRED = 'expired', // invitation expired
}
